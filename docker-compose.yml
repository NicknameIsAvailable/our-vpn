version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_NAME: api
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=development
      - APP_NAME=api
    volumes:
      - .:/app
      - /app/node_modules
